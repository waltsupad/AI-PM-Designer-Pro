# 網頁生成技術方案與自動部署規劃

## 🛠 網頁生成技術方案

### 方案 A：純 JavaScript/TypeScript 生成 HTML（推薦）

**技術架構：**
```
┌─────────────────────────────────────┐
│  瀏覽器端生成 (Client-Side)         │
├─────────────────────────────────────┤
│  1. 資料收集                        │
│     - 從 App State 取得所有資料     │
│     - 收集已生成的圖片 (base64)    │
├─────────────────────────────────────┤
│  2. HTML 模板生成                  │
│     - 使用 Template Literals        │
│     - 或使用模板函數                │
│     - 動態填入內容                   │
├─────────────────────────────────────┤
│  3. CSS 樣式生成                   │
│     - 內嵌 CSS（Tailwind 或自訂）  │
│     - 響應式設計                    │
│     - 品牌色彩動態應用               │
├─────────────────────────────────────┤
│  4. 圖片處理                        │
│     選項 A: Base64 內嵌（單一檔案） │
│     選項 B: 生成資料夾結構          │
├─────────────────────────────────────┤
│  5. 輸出檔案                        │
│     - 使用 Blob API 生成 HTML       │
│     - 提供下載功能                  │
└─────────────────────────────────────┘
```

**優點：**
- ✅ 不需要後端伺服器
- ✅ 完全在瀏覽器端執行
- ✅ 單一 HTML 檔案，易於分享
- ✅ 技術簡單，易於維護

**缺點：**
- ⚠️ 單一檔案可能較大（如果內嵌所有圖片）
- ⚠️ 需要處理大量 base64 資料

**實作範例：**
```typescript
// utils/webPageGenerator.ts
export const generateLandingPageHTML = (
  data: WebPageData
): string => {
  const html = `
<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>${data.productAnalysis.name} - ${data.route.route_name}</title>
  <style>
    /* 內嵌 CSS */
    ${generateCSS(data.route)}
  </style>
</head>
<body>
  <!-- Hero Section -->
  <section class="hero">
    <img src="${data.images.get('main_lifestyle')}" alt="${data.contentPlan.items[1].title_zh}">
    <h1>${data.route.headline_zh}</h1>
    <p>${data.route.subhead_zh}</p>
  </section>
  
  <!-- Features Section -->
  <section class="features">
    ${generateFeaturesSection(data)}
  </section>
  
  <!-- ... 其他區塊 ... -->
  
  <script>
    // 內嵌 JavaScript（如果需要互動功能）
    ${generateJavaScript()}
  </script>
</body>
</html>
  `;
  
  return html;
};
```

---

### 方案 B：使用 React 生成靜態 HTML

**技術架構：**
- 使用 `react-dom/server` 的 `renderToString`
- 在瀏覽器端執行（需要額外的建置步驟）

**優點：**
- ✅ 可以重用現有的 React 組件
- ✅ 組件化開發

**缺點：**
- ⚠️ 需要額外的建置工具
- ⚠️ 在瀏覽器端執行 SSR 較複雜
- ⚠️ 檔案大小較大

**結論：** 不推薦，過於複雜

---

### 方案 C：使用模板引擎（Handlebars/EJS）

**技術架構：**
- 使用 Handlebars.js（瀏覽器版本）
- 定義 HTML 模板
- 動態填入資料

**優點：**
- ✅ 模板與邏輯分離
- ✅ 易於維護
- ✅ 支援部分模板（Partials）

**缺點：**
- ⚠️ 需要引入額外的套件
- ⚠️ 增加 bundle 大小

**結論：** 可選，但方案 A 更簡單

---

## 🎯 推薦方案：方案 A（純 JavaScript/TypeScript）

**實作步驟：**

1. **建立模板函數**
   ```typescript
   // utils/webPageGenerator.ts
   export const generateLandingPage = (data: WebPageData): string => {
     return generateHTMLTemplate(data);
   };
   ```

2. **圖片處理策略**
   - **單一檔案模式**：所有圖片轉為 base64 內嵌
   - **多檔案模式**：生成 HTML + images 資料夾（使用 JSZip）

3. **樣式系統**
   - 使用 Tailwind CDN（最小化）
   - 或生成自訂 CSS（根據品牌色彩動態生成）

4. **輸出方式**
   ```typescript
   const html = generateLandingPage(data);
   const blob = new Blob([html], { type: 'text/html' });
   const url = URL.createObjectURL(blob);
   // 提供下載或預覽
   ```

---

## 🚀 自動部署方案

### 方案 1：GitHub Pages（推薦）

**技術架構：**
```
┌─────────────────────────────────────┐
│  1. 生成 HTML 檔案                  │
├─────────────────────────────────────┤
│  2. 使用 GitHub API                 │
│     - 建立新的 repository           │
│     - 或推送到現有 repository       │
│     - 上傳 HTML 和圖片檔案          │
├─────────────────────────────────────┤
│  3. 啟用 GitHub Pages               │
│     - 自動或手動啟用                │
│     - 獲得公開 URL                  │
└─────────────────────────────────────┘
```

**實作方式：**
- 使用 GitHub REST API 或 GitHub.js
- 需要 GitHub Personal Access Token
- 可以自動建立 repository 並部署

**優點：**
- ✅ 免費
- ✅ 自動 HTTPS
- ✅ 自訂網域支援
- ✅ 版本控制

**缺點：**
- ⚠️ 需要 GitHub 帳號
- ⚠️ 需要 API Token（需使用者授權）

---

### 方案 2：Netlify Drop（最簡單）

**技術架構：**
```
┌─────────────────────────────────────┐
│  1. 生成 HTML + 資源檔案            │
├─────────────────────────────────────┤
│  2. 使用 JSZip 打包                 │
├─────────────────────────────────────┤
│  3. 使用 Netlify Drop API           │
│     - 上傳 ZIP 檔案                 │
│     - 自動部署                      │
│     - 獲得公開 URL                  │
└─────────────────────────────────────┘
```

**實作方式：**
- 使用 Netlify Drop API（不需要認證）
- 或使用 Netlify CLI（需要認證）

**優點：**
- ✅ 非常簡單
- ✅ 不需要帳號（Drop 模式）
- ✅ 自動 HTTPS
- ✅ 全球 CDN

**缺點：**
- ⚠️ Drop 模式 URL 是隨機的
- ⚠️ 需要帳號才能自訂網域

---

### 方案 3：Vercel（推薦）

**技術架構：**
- 使用 Vercel API
- 上傳專案檔案
- 自動建置與部署

**優點：**
- ✅ 快速部署
- ✅ 自動 HTTPS
- ✅ 全球 CDN
- ✅ 支援自訂網域

**缺點：**
- ⚠️ 需要 Vercel 帳號
- ⚠️ 需要 API Token

---

### 方案 4：Cloudflare Pages（推薦）

**技術架構：**
- 使用 Cloudflare Pages API
- 上傳檔案
- 自動部署

**優點：**
- ✅ 與現有 Cloudflare 整合
- ✅ 快速部署
- ✅ 全球 CDN

**缺點：**
- ⚠️ 需要 Cloudflare 帳號
- ⚠️ 需要 API Token

---

## 🎯 推薦組合方案

### 階段 1：基礎生成 + 下載（MVP）
- **網頁生成**：方案 A（純 JavaScript）
- **輸出方式**：下載 HTML 檔案
- **圖片處理**：Base64 內嵌（單一檔案）

**優點：**
- 技術簡單
- 立即可用
- 不需要額外服務

### 階段 2：自動部署（進階）
- **網頁生成**：方案 A（純 JavaScript）
- **輸出方式**：下載 + 自動部署選項
- **部署平台**：Netlify Drop（最簡單）或 GitHub Pages

**實作流程：**
```
使用者點擊「生成並部署網頁」
  ↓
生成 HTML + 資源檔案
  ↓
打包為 ZIP
  ↓
選擇部署平台：
  - Netlify Drop（一鍵部署，獲得 URL）
  - GitHub Pages（需要授權，獲得自訂 URL）
  - 下載檔案（本地使用）
```

---

## 📋 技術實作細節

### 1. 網頁生成函數結構

```typescript
// utils/webPageGenerator.ts

interface WebPageData {
  productAnalysis: ProductAnalysis;
  route: MarketingRoute;
  contentPlan: ContentPlan;
  generatedImages: Map<string, string>; // item.id -> base64 image
}

export const generateLandingPage = (
  data: WebPageData,
  options?: {
    template?: 'minimal' | 'modern' | 'classic';
    imageMode?: 'inline' | 'external';
    includeAnalytics?: boolean;
  }
): {
  html: string;
  assets?: Map<string, Blob>; // 如果 imageMode === 'external'
} => {
  // 1. 選擇模板
  const template = selectTemplate(options?.template);
  
  // 2. 處理圖片
  const images = processImages(data.generatedImages, options?.imageMode);
  
  // 3. 生成 HTML
  const html = template.render({
    product: data.productAnalysis,
    route: data.route,
    content: data.contentPlan,
    images: images,
  });
  
  // 4. 生成 CSS
  const css = generateCSS(data.route);
  
  // 5. 組合完整 HTML
  return {
    html: combineHTML(html, css),
    assets: options?.imageMode === 'external' ? images.assets : undefined,
  };
};
```

### 2. 自動部署函數結構

```typescript
// utils/webPageDeployer.ts

export const deployToNetlify = async (
  html: string,
  assets?: Map<string, Blob>
): Promise<{ url: string }> => {
  // 1. 打包檔案
  const zip = await createZip(html, assets);
  
  // 2. 上傳到 Netlify Drop
  const response = await fetch('https://api.netlify.com/api/v1/sites', {
    method: 'POST',
    body: zip,
  });
  
  // 3. 返回部署 URL
  return { url: response.site_url };
};

export const deployToGitHubPages = async (
  html: string,
  assets: Map<string, Blob>,
  token: string,
  repoName: string
): Promise<{ url: string }> => {
  // 1. 使用 GitHub API 建立 repository
  // 2. 上傳檔案
  // 3. 啟用 GitHub Pages
  // 4. 返回 URL
};
```

---

## 🔐 安全性考量

### API Token 管理
- **GitHub/Vercel/Cloudflare**：需要使用者提供 Personal Access Token
- **儲存方式**：僅存在記憶體中，不儲存到 localStorage
- **授權流程**：OAuth 授權（未來可考慮）

### Netlify Drop
- **優點**：不需要認證
- **缺點**：URL 是隨機的，無法自訂

---

## 📊 比較表

| 方案 | 技術難度 | 部署難度 | 成本 | 自訂網域 | 推薦度 |
|------|---------|---------|------|---------|--------|
| **純 JS 生成 + 下載** | ⭐⭐ | ⭐ | 免費 | ❌ | ⭐⭐⭐⭐ |
| **+ Netlify Drop** | ⭐⭐ | ⭐⭐ | 免費 | ❌ | ⭐⭐⭐⭐⭐ |
| **+ GitHub Pages** | ⭐⭐⭐ | ⭐⭐⭐ | 免費 | ✅ | ⭐⭐⭐⭐ |
| **+ Vercel** | ⭐⭐⭐ | ⭐⭐⭐ | 免費 | ✅ | ⭐⭐⭐⭐ |
| **+ Cloudflare Pages** | ⭐⭐⭐ | ⭐⭐⭐ | 免費 | ✅ | ⭐⭐⭐⭐ |

---

## 🎯 最終推薦

### MVP 階段（立即實作）
1. **網頁生成**：純 JavaScript/TypeScript 模板
2. **輸出方式**：下載單一 HTML 檔案（圖片內嵌 base64）
3. **樣式**：內嵌 CSS（使用 Tailwind CDN 或自訂 CSS）

### 進階階段（後續擴展）
1. **自動部署**：整合 Netlify Drop API（最簡單）
2. **多檔案模式**：生成 HTML + 圖片資料夾（使用 JSZip）
3. **多平台部署**：支援 GitHub Pages、Vercel、Cloudflare Pages

---

## 💡 總結

**網頁生成技術：**
- ✅ 使用純 JavaScript/TypeScript 生成 HTML 字串
- ✅ 使用 Template Literals 或模板函數
- ✅ 圖片可內嵌 base64 或生成外部檔案
- ✅ 使用 Blob API 提供下載

**自動部署：**
- ✅ **可以**自動部署
- ✅ 推薦使用 Netlify Drop（最簡單，不需要認證）
- ✅ 進階選項：GitHub Pages、Vercel、Cloudflare Pages（需要 API Token）

**實作優先級：**
1. 先實作網頁生成 + 下載功能
2. 再整合 Netlify Drop 自動部署
3. 最後加入其他平台選項

